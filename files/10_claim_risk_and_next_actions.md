14. The Claim, and What Would Falsify It
The claim is proven only if all of the following hold:

Stage 0b resolves ≥90% automatically with all 9 never_merge pairs kept apart. ✅ 100%, 9/9
Gate 1 picks a normalisation that survives the composition-skew check and wins on LOCO, not on held-out cells. ✅ V3
Gate 1b agreement ≥0.90 with the hard cases correct and the nesting graph asserts acyclic. ✅ 0.928, 4/5 required + 1 waived, DAG asserted — with the caveats in §7
Stage 2 dynamic tokens beat the fixed core. ✅ 0.1963 vs core-9 control 0.1707 vs Gate 1 0.169 — both conditions of Gate 2 check 3 hold (H3 closed). Note the STAGE fails: Gate 2 check 1 fails on 1 kept pair (Keren MKI67, −0.0117) and that FAIL is recorded, not repaired (D-34). This claim row rests on check 3, which is the row that tests it.
LOCO macro-F1 beats 0.630. ❌ THE DECLARED COMPARISON IS NOT AVAILABLE, and saying so is part of the claim. Gate 6 measures 0.3901 over 22 data-derived clusters; the old pipeline's 0.630 is mean L1 macro-F1 over ~10 hand-written classes. Different label space, different class count, different difficulty - they are not the same quantity and no honest arithmetic makes them one. Report both with their class counts side by side and state that the comparison is indicative only. Do NOT present 0.3901 as a loss to 0.630, and do NOT dress it as a win. Two further numbers belong beside it: 0.4328 over the 15 LEARNABLE clusters (10 of 25 exist in only one training cohort and score 0.000 by construction under LOCO), and the in-distribution ceiling 0.68-0.79, whose distance from the test number - a mean gap of +0.3179 - is the real measured result of this pipeline
The ferguson zero-shot number is reported whether or not it is good. ✅ DONE 2026-08-11 — **0.3309** over 22 reliable clusters, majority 0.0237, random 0.0562, and it is reported here exactly as it came out. Three numbers, not one (D-48): A 0.3309 shipped · B1 0.3080 clean-37 · B2 0.4240 clean-22. Two declared predictions were tested: the ordering prediction HELD, the predicted range 0.35-0.55 was WRONG (D-50). The leak measured by D-46 turned out to PENALISE the shipped number by 0.0931, not flatter it (D-51), so the headline is conservative. THE ROW THAT MATTERS MORE THAN THIS ONE: transfer works for cell types several cohorts agree on (mean F1 0.5291 over 5 labels) and fails completely for types carried by one or two (mean 0.0014 over 4), r = 0.843 — Gate 6's support law replicating on an unseen machine and tissue. All three runs hit the epoch ceiling, so these are a LOWER BOUND.
(The Stage 4 shuffle control is deferred with Stage 4.)

Comparison baselines: old pipeline 0.630 mean L1 macro-F1; published MAPS cross-dataset 0.5–0.6. State the cluster count next to any number — the class set is now data-derived, so the comparison is indicative, not exact.

15. Risk Register
Risk	Why it matters	Mitigation	Status
HGNC/UniProt return a wrong or ambiguous hit	A silently wrong marker id corrupts everything downstream	Field-scoped queries, spellings must agree, ambiguous → review queue	❌ REOPENED 2026-08-14 — THE MITIGATION DOES NOT COVER THIS CASE AND THE OLD STATUS WAS WRONG. Three wrong ids are now measured (D-55): `SMA` → SMN1 via HGNC PREVIOUS-symbol (not ACTA2), `FSP1` → AIFM2 (not S100A4), `CD31-vWF` → VWF (not PECAM1). All three are UNAMBIGUOUS single hits, so "spellings must agree" and the review queue never fire — the resolver is confidently wrong, which is the worst case this row was written for. `HGNC:11117|pan|none` (SMN1) is a live slot in the frozen 99-triple vocabulary the shipped model was trained on, so it cannot be repaired without re-earning every gate from Stage 2. Gate 0b check 3c does not catch it: 3c only guards DECLARED non-protein channels. THE FIX THAT WAS AVAILABLE ALL ALONG: the datasets ship their own antibody panels (e.g. Datasets/Danenberg/AbPanel.csv gives target + clone), and checking against them is external, free, and would have caught all three. WAS: ✅ handled; 0 leaks measured
Marker resolution drifts between runs	APIs change; results stop reproducing	Resolve once, cache, and Gate 0b requires a byte-identical offline re-run	✅ verified
All normalisation variants fail the skew check	Composition-skewed slides could break every option	Report it and use V3, which cannot invent a negative population	✅ V3 passed
Gate 1b agreement < 0.90	The biggest single change; if wrong, everything downstream is wrong	Inspect disagreements first — some will be the hand mapping being wrong. Fallback: hand mapping for the 4 old cohorts + automatic for the new	✅ 0.928
Too many label pairs below the evidence floor	Sorin has only 17 markers; the graph could fragment	Gate 1b prints the never-comparable share and sweeps k. Marker-rich cohorts bridge	✅ 100% directly comparable — but this also means the bridging mechanism was never exercised (§7 gap 1)
The label graph has cycles or contradictory nesting	A cyclic "hierarchy" is not a hierarchy	Margin rule kills 2-node cycles; SCC contraction gives a DAG by theorem; assert is_directed_acyclic_graph	✅ asserted — but weak evidence, the graph was too sparse to test it (§7 gap 2)
FiLM memorises cohort instead of slide drift	Would re-inject what Stage 3 removes	Within-cohort standardisation, ±30% output bound, V2a vs V2b raced on LOCO	✅ moot — FiLM lost
[ABSENT] tokens act as a panel fingerprint	Would hand Stage 3's adversary a problem it should not have	Gate 2 check 4: measured ablation + cohort probe	✅ measured, H1 closed — but NOT as expected. The cohort probe hit 1.000 for BOTH arms, so absent tokens are not what makes the panel identifiable; the marker VALUES already are. Dropping them would not have helped and would have cost 0.0293 cross-cohort R². The risk is real but it is Stage 3's to carry either way, which is what the adversary is for
A new cohort's labels are too coarse to cluster	e.g. only immune/stromal/tumour	Its labels join the coarse clusters; it contributes cells to training but not fine-level scoring. Reported per cohort	⚪ not yet hit
Prototypes collapse during training	Two classes silently merge and F1 still looks fine	Gate 6 collapse guard: track minimum pairwise distance, freeze and log violations	✅ MEASURED, NO BREACH - and the opposite happened. The minimum pairwise prototype distance ROSE from an init 0.0717 to 0.9571 and the median nearest-neighbour distance from 0.1288 to 1.1040, so the prototypes spread by about 13x rather than merging. No pair was ever frozen. The guard cost nothing and is worth keeping for Stage 7
Gate 3 fails at every λ	Cohort and biology may still be too tangled	Fall back to λ=0 and report it	✅ HIT, and the fallback worked exactly as written. No λ beat λ=0; the gate PASSES on λ=0 at 0.3642 (D-36). Nothing was lost — Stage 6 inherits the encoder it would have had anyway
The adversary looks like it is working when it is not	A collapsing discriminator is the standard evidence of success in this literature, and it is not sufficient	The fresh probe (D-15, made computable by D-35): freshly initialised, never adversarially trained, scored on held-out cells	✅ CAUGHT ONE. Co-trained bits fell to −3.29, below chance, while the fresh probe still recovered 89%. On the co-trained number alone this run would have been written up as a success. ✅ AND THE OBVIOUS OBJECTION IS NOW CLOSED. Stage 3b matched the discriminator to the probe's capacity: hiding got WORSE, not better — fresh bits at λ=0.3 rose to 2.92 from 2.65 while the co-trained head fell to −4.53 from −2.02. The encoder was not beaten by a weak critic (D-43, H5 closed)
The adversary is aimed at the wrong variable	Erasing patient identity erases tumour biology, and it is patients that dominate the classes	Domain becomes slide-nested-within-patient, so only slides sharing a patient are distinguished — technical by construction	❌ TESTED AND DISPROVEN. The nested domain is WORSE than λ=0 at every lambda (−0.0183, −0.0306, −0.0857) and the Sorin gain that motivated it did not replicate: +0.0752 became +0.0070 under nesting while persisting at +0.0743 in the arm that does NOT nest. The r = −0.852 was flagged at n=5, p≈0.07 and did not survive. Kept as a record rather than deleted (D-43, H6 closed). The stronger finding is the inverse: arm B removed the MOST slide information (26.4% retained vs 38.0%) and scored the WORST F1 — batch is not the bottleneck
Sorin has only 17 markers	Could drag the shared panel down	It is the panel-mismatch stress test. Track its fold separately; the masked-token design exists for this	✅ Stage 2 — and it earned its keep. Sorin is the fold that decided the architecture: Arm A scores −0.1033 there against Arm B's +0.0388, a 0.1421 gap that is invisible in every other measurement (D-30). Sorin's fold remains the weakest (0.0388) and should stay reported separately
A declared gate rule turns out to be defective	Four of Gate 2's rules were (D-28, D-29, D-30, and check 1 under D-34), plus Gate 3's fresh probe (D-35) and, before it, D-15. Revising rules after seeing results can quietly become fitting the gate to the answer	Every replacement is dated, reasoned, and kept beside the original in the gate's expect CSV marked REPLACED — never deleted. A replacement that would flip a verdict in its own favour is refused: that is exactly why Gate 2 check 1 stands as FAIL	🟡 ongoing discipline, not a solved risk. Watch it in Gates 6 and 7
Breast tissue imbalance	—	Deliberate: same tissue on different machines separates batch from biology. Report per-tissue results	⚪ weakened by D-24 (Danenberg out of training)
Kaggle quota 30 h/week	The final run could be cut off	Checkpoint every epoch; the LOCO loop resumes fold by fold	✅ not a binding constraint. Measured GPU use so far: Gate 3 39.8 min + Stage 3b 80.2 min + Gate 6 73.5 min = 3.2 h total. Everything still outstanding (Stage 7, the seed re-runs, the check 6 repair) fits inside about 4 more hours. TIME is the constraint, not quota
Stroma clusters are unreliable	8.5% of cells, 3 clusters, 23 labels	Flagged in label_map.csv; Stage 7 must exclude or separately report	✅ flagged, ⚪ Stage 7 must honour it
No gate has ever been scored with a confidence interval	Every verdict in this project from Gate 1 on is a mean over 5 folds at one seed against a threshold. That is a decision procedure, not evidence. It has already produced one published -looking result that was noise (D-38)	Re-run the shipped configuration at 5 seeds x 5 folds and report mean ± 95% CI with a paired test on every headline	❌ HIT, AND IT HAS ALREADY BITTEN. Retrospective tests: Gate 6 check 4 p = 0.460 with a CI four times the margin and all of it from one fold; Gate 6 check 3 (VICReg) p = 0.191; across Gate 3's entire lambda sweep the only significant contrast is lambda=0.3 and it is WORSE. The gates still pass on their declared rules - what fails is the SENTENCES written about them. See D-44, H9
The frozen holdout helped shape the label space	Stage 1b clustered all 6 cohorts together, so ferguson's marker signatures are in the distance matrix that produced the 25 clusters Stage 7 scores in. Not a training leak; still a leak	Do NOT rebuild - with a month left that would change the cluster count and invalidate every number in the thesis. Measure it instead: re-cluster on 5 cohorts, compare, report the agreement. Same move as D-27's core-9 control	❌ MEASURED 2026-08-11 AND IT FAILS ITS DECLARED THRESHOLDS - cell-weighted ARI 0.5447 against a 0.90 bar. The mechanism is narrower than the verdict: the signatures are provably untouched (SIM/EV bit-identical) and at a matched cut the ARI is 0.9828, so ferguson changed the GRANULARITY the guards allow (25 clusters instead of 37), not which labels are alike. The shipped cut is still legal without ferguson. Direction of the bias is KNOWN and it favours the result - coarser is easier - so it must be stated beside Stage 7's number. See D-46, H10, reports/s1b_control_ferguson.md
A gate check can be structurally void and still print a number	Gate 6 check 6 compared confidence-on against confidence-off on the one fold where the only cohort with confidence was held OUT of training. Both arms were the same computation and the delta printed 0.0000	A check whose two arms could be bit-identical must assert they are not. Cheap and general	❌ HIT. Recorded as UNSCORED, not passed (D-45). Repair is 2 fits holding out CRC instead
16. Immediate Next Actions (rewritten 2026-08-11 after GATE 6 and a full statistical review)
ONE MONTH LEFT. The binding constraint is writing time, not GPU - everything below is under 4 h on a T4.
Do NOT spend it raising 0.3901. Three separate experiments say architecture changes do not help here, and the number is not the contribution.

--- REPRIORITISED 2026-08-14. Read this block before the 2026-08-11 steps below it. ---
THE PROJECT HAS NO VALID EXTERNAL COMPARISON, and as of today BOTH comparison points this file used to carry are withdrawn: the old pipeline's 0.630 (a 3-class L1 number against a 22-cluster one, D-49) and "published MAPS cross-dataset 0.5-0.6", which does not exist — MAPS ran no cross-dataset experiment at all (D-56). What the literature actually reports, for scale: VirTues (Nature 2026) zero-shot macro-F1 0.691 CODEX / 0.607 Orion on technologies withheld from training; DeepCell Types leave-one-dataset-out accuracy 0.551, with MAPS at 0.427 in the same setting. Against 0.3309 (ferguson) and 0.2161 (Danenberg) this project loses on macro-F1 and should stop trying to win there.
WHAT THE PROJECT CAN STILL WIN. VirTues, DeepCell Types, RIBCA and MAPS are all HANDED the label vocabulary; Stage 1b DERIVES it. That is the contribution (D-49) and the comparison that tests it is H13b, not macro-F1.
STEP A - DONE 2026-08-14, AND IT PASSES. pipeline2/s10_external.py, 25 fits, ~41 min CPU (H13a / D-57). Stage 6 0.3901 beats every baseline: best is maps_core9 at 0.3447, paired margin +0.0454 [+0.0054, +0.0766] on 4 of 5 folds. The project now has an interpretable number for the first time. TWO THINGS TO SAY HONESTLY IN THE WRITE-UP: the margin is SMALL and conflates panel width with architecture (99 markers vs 10), and gradient boosting on zero-filled 99 features beats the shipped model on 2 of 5 folds. THE STRONGEST ROW IS NOT THE HEADLINE: maps_core9 0.3447 → maps_full99 0.2116 is external proof that zero-filling unmeasured markers hurts a published architecture badly, which is exactly what Stage 2 was built to avoid and had never been tested outside this codebase.
STEP A-FOLLOW-UP - NEW, ONE CHEAP RUN. The +0.0454 cannot separate architecture from panel width. Run Stage 6 RESTRICTED TO THE CORE 10 MARKERS on the same folds; against maps_core9 that is a like-for-like architecture comparison. If it does not beat MAPS there, the honest claim becomes "the gain comes from using the whole panel properly", which is a BETTER thesis sentence than an architecture claim and is directly supported by the maps_full99 collapse.
STEP B - BUILT, NEEDS A GPU. pipeline2/s11_spacetest.py (H13b / D-58) — the thesis claim tested as an outcome. 9 fits. It already returned one result without training: Stage 1b cannot choose a granularity from 3 cohorts, so the claim must be split into boundary derivation (testable, survives) and granularity selection (fails below 5 cohorts).
STEP C - ✅ DONE 2026-08-14, minutes on CPU. External validation of the 25 clusters (H15) — see D-59 and `reports/s12_ontology.md`. Built as Stage 12 rather than as a lookup: all 106 labels mapped to the Cell Ontology (Phillips and Sorin for the first time — the old `_validation/hand_mapping_reference.csv` covered only 4 cohorts and 69 labels), then scored against CL's hierarchy, CL's own published levels, and CellMarker 2.0 for markers. HuBMAP ASCT+B, which this line named, was unreachable (404/500 on every endpoint) and CellMarker substitutes.
  WHAT IT BOUGHT. **ARI 0.4400 [0.3662, 0.6624] per label · 0.5725 reliable-only · 0.934 cell-weighted against the exact CL term, beating a 1000-shuffle null at p99.9 at all four published levels.** The comparison that matters: Gate 1b scored the same clusters against the project's OWN hand mapping at 0.596 / 0.962, and an ontology nobody here wrote gives 0.440 / 0.934 over a LARGER label and class set. The 0.928 was not an artefact of marking your own homework. **CL places `Phillips|tumor cells` and `CRC|tumor cells` DISTANT and Stage 1b splits them** — the hardest case in `panel/gate1b_expect.csv`, refereed from outside. First gate in the project with a confidence interval and a permutation null.
  WHAT IT COST, AND BOTH BELONG IN THE WRITE-UP. The nesting layer failed its first external test (0.282 of CL's parent-child pairs represented, bar 0.60) — H2 is answered and the answer is no. Same-term merging also missed (0.645, bar 0.90). Only 0.228 of labels resolved to CL automatically, so the defensible sentence is **"the derived clusters agree with a published ontology's STRUCTURE"**, never "the labels were mapped without human input".
  STILL OPEN AFTER STEP C: granularity (H11 / D-47) is untouched by this and deliberately out of its scope.
STEP D - FIX config.SPECS['Danenberg']['role'] (H14). One line. Until it changes, any Gate 3 / Gate 6 re-run on this machine silently trains on SIX cohorts.
STEP E - step 3 (s8_seeds.py, 55 fits, H9) drops BELOW steps A-C. Confidence intervals on a number with no external comparison are polish; get the comparison first.
--- end 2026-08-14 block ---

STEP 0 - DONE 2026-08-11. Gate 6 recorded: D-44 (PASS, with the p = 0.460 caveat stated beside it), D-45 (check 6 is a no-op), section 7 gap 4, H9, H10, and the status/risk rows in this file and files/09.

STEP 1 - DONE 2026-08-11, and it FAILS (D-46). pipeline2/s1b_control.py, 3 min on CPU, thresholds declared first in panel/gate1b_control_expect.csv and NOT moved afterwards. Cell-weighted ARI 0.5447 against a 0.90 bar. The mechanism is granularity, not structure: the signatures are bit-identical, and at a matched cut the ARI is 0.9828 with 18 of 22 control clusters nesting cleanly. ferguson narrows the usable cut window from 0.650-0.850 to 0.750-0.800, giving 25 clusters instead of 37 - but the shipped cut is still inside the 5-cohort window. Bias direction is known and FAVOURS the result, since coarser is easier.
STEP 1 CONSEQUENCE - a decision is now owed before step 2. Either (1a) DISCLOSE: run Stage 7 in the shipped 25-cluster space, cite this control, state the bias direction - costs nothing, and an examiner can still ask the question; or (1b) ALSO REPORT A CLEAN-PROTOCOL NUMBER: freeze the control's 37-cluster 5-cohort space, assign ferguson's 9 labels into it from their marker signatures alone, then train and score there as a SECOND number - fully leak-free, it is the deployment procedure this project actually claims, and it costs one extra training run (~15 min on a T4) plus a prototype rebuild. 1b is the stronger thesis; 1a is the cheaper one. It also surfaced D-47 / H11, a separate defect in how the cut is chosen at all.

STEP 2 - STAGE 7. BUILT AND CPU SMOKE-TESTED 2026-08-11, NOT YET SCORED. pipeline2/s7_spaces.py + pipeline2/s7_eval.py + panel/gate7_expect.csv + kaggle/stage7.ipynb. 3 fits, ~20-25 min on a T4. Trains on all 5 training cohorts (not LOCO) with Gate 6's shipped arm, nothing re-tuned, then scores ferguson as pure test.
  THREE numbers per the user's decision (D-48): A = shipped 25 clusters, disclosed as not blind to the holdout; B1 = clean 37 clusters at the 5-cohort rule's own cut; B2 = clean 22 clusters at the SHIPPED cut, which prices the leak with granularity held fixed.
  Thresholds AND a predicted range were committed first: 0.35-0.55 on space A, with the reasoning written down so it can be wrong. A declared ordering prediction too - B2 >= A >= B1 from cluster count alone - which corrects a loose line in D-46: the leak made A coarser than B1 but FINER than B2, so 'the bias favours A' holds only against B1.
  TWO RESULTS ALREADY IN, before any model ran: ferguson EP is NOVEL in space B2 (no cluster admits it at distance 0.8120 vs a cut of 0.800; 14,170 cells, dropped from the macro-F1 and reported), and EP fuses with GC in space B1. At one legal cut EP is a novel class, at another it is a granulocyte - that is D-47 arriving in the final answer.
  REMAINING FOR THE USER: run kaggle/stage7.ipynb. The dataset must be REBUILT and re-uploaded first (94.00 MB, up from 77.84) because it now carries the holdout's value table and the two clean spaces (D-49).

STEP 2 IS DONE 2026-08-11 — see the claim row in section 14 and D-50/D-51. The single most valuable follow-up is now cheap and obvious: the support law is measured on 9 labels and is the strongest result in the project, so it deserves the confidence intervals of STEP 3 more than anything else does.

STEP 2b - STAGE 7b, NOT BUILT: the abstain accuracy-vs-coverage curve and the novel-class AUROC, both designed in files/05 section 4.11. Both reuse Stage 7a's fitted models with NO retraining, so they are minutes of compute. Deliberately separated so the headline number is not blocked behind them.

STEP 3 - CONFIDENCE INTERVALS. CODE COMPLETE AND CPU SMOKE-TESTED 2026-08-11, NOT YET RUN. pipeline2/s8_seeds.py + kaggle/stage8.ipynb. 55 fits, ~3.5-4 h on a T4: 5 seeds x 5 LOCO folds x 2 heads, plus 5 seeds on ferguson. Resumes fit by fit. It settles the Gate 6 LOCO headline, Gate 6 check 4 (the sentence D-44 banned), and the support law. THIS IS THE ONLY THING LEFT THAT NEEDS THE GPU, and it is the biggest remaining methodological hole (H9).

STEP 4 - DONE 2026-08-11, no compute (D-53, reports/s8_perclass.md). Gate 6 LOCO 0.3901 reported → 0.4606 learnable-only; 9 of 55 class-folds unlearnable by construction. The support law on the LOCO folds is a monotone ladder - 0 cohorts 0.0000 · 1 cohort 0.0921 · 2 cohorts 0.2231 · 3+ cohorts 0.6361, with not one of 29 3+ class-folds at zero, r = 0.781. H7 CONFIRMED AND CLOSED: CRC ranks 2nd of 5 on learnable classes, not last.

STEP 5 - DONE 2026-08-11 (D-52, reports/s7b_abstain.md). Abstain works and has a clear operating point (macro-F1 0.3309 → 0.4204 at 35% coverage). Novel-class detection FAILS at AUROC 0.578 and is reported as a failure, as check 6 declared in advance it must be. The D-45 repair (confidence on/off holding out CRC, 2 fits) is still not run and is the one cheap thing outstanding.

STEP 6 - WRITE. This is now the whole remaining job apart from step 3's GPU run. THE SPINE OF THE THESIS, in the order the evidence supports it:
  (1) Label harmonisation across cohorts CAN be learned from marker profiles - 0.928 against a hand mapping, no ontology, no dictionary. This is the novel claim and it holds.
  (2) Given that, cross-cohort transfer is governed by ONE law, and it is the most reproducible result in the project: a cell type transfers if several cohorts independently agree on it, and does not otherwise. 0.000 / 0.092 / 0.223 / 0.636 by contributing cohort count on the LOCO folds (r = 0.781); 0.0014 against 0.5291 on ferguson (r = 0.843); measured in three label spaces; and it predicted the ferguson failures before they were scored.
  (3) Domain-adversarial training does not fix it, for a MEASURED reason - it hides batch signal rather than removing it (fresh probe, D-37), and removing it harder made transfer worse (D-43). Two rescue hypotheses, both tested, both failed.
  (4) The deliverable can rank its own predictions and decline the weak ones (D-52), and CANNOT notice a cell type it has never seen (AUROC 0.578). Say both.
  Lead with (2). It is the finding, it is replicated, and it tells the next person what to do: add cohorts that overlap on the types you care about, not more markers or more architecture.
WAS: Lead with the negative result and the fresh-probe method, not with macro-F1. The defensible framing is: label harmonisation across cohorts CAN be learned from marker profiles (0.928); given that, cross-cohort transfer is still limited by domain shift (+0.3179 val-to-test gap); and domain-adversarial training does not fix it, for a measured reason - it hides batch signal rather than removing it, and removing it harder makes transfer worse.

EXPLICITLY DROPPED for the remaining month: Stage 4, any further adversarial work, architecture search, and chasing the old pipeline's 0.630 (see the claim row in section 14 - that comparison is not available).
Still outstanding from earlier, cheap, do only if the above finishes: the section 7 gap-1 ablation (co-expression / prevalence), the H4 slide-split reseed, and reports/DESIGN_learning_paradigm.md, which was specified in an earlier plan, never written, and whose draft text still says "Gate 1b fails" - stale, Gate 1b passes at 0.928.

--- SUPERSEDED 2026-08-11 by the list above, kept for the record ---
16b. Next actions as they stood after GATE 3
~~Stage 3b~~ DONE 2026-08-11. Both rescue hypotheses fail; λ=0 stands and the negative result is now defensible (D-43). The finding that survives is stronger than the verdict: arm B removed the MOST slide information (26.4% retained vs 38.0%) and scored the WORST macro-F1 — removing batch signal more successfully made transfer worse.
Run GATE 6, 16 fits, ~30–40 min on a T4 — pipeline2/kaggle/stage6.ipynb. Code complete and CPU smoke-tested.
⚠️ WRITE-UP CAVEAT, accepted deliberately (user decision, 2026-08-11): Gates 3 and 3b ran on the 88-triple vocabulary from before D-39, with a partial warm start. Stage 6 onward uses the canonical 99. Gate 3's 0.3642 is therefore NOT comparable with any later number and must not be placed in a shared table without saying so. Gate 6 re-measures Stage 3's linear head inside its own run for exactly this reason. Re-running Gate 3 at 99 would cost ~40 min and remove the caveat; it was judged not worth it because Stage 3b's conclusions are internal to its own arms.
Then Stage 6 (s6_train.py) — and AIM IT AT THE RIGHT PROBLEM. The evidence says batch is not the bottleneck: λ=0 retains the most slide information and has the best transfer. CRC is the worst fold (0.2770) despite the largest cohort and second-richest panel, which points at class imbalance and label granularity (H7), not batch. That is where the prototype loss and class balancing should go.
Do NOT spend more time on adversarial training beyond Stage 3b. The ceiling is small: λ ≤ 0.03 sit inside fold noise. If more domain robustness is wanted later, CORAL/MMD (a fixed distance, so there is nothing to fool) or Group DRO (targets the worst fold directly) fit this failure mode better than another critic.
Decide whether H4 is worth testing: re-run Gate 2's Arm B holdout at a different slide-split seed (~85 min) to tell an unlucky 8-slide Keren draw from a real property of Keren.
Report the ferguson zero-shot number at Gate 7 whether or not it is good. It remains the only untested thing: a machine, tissue and panel the pipeline has never seen.
Still outstanding from earlier: the cheap §7 gap-1 ablation (co-expression / prevalence), and reports/DESIGN_learning_paradigm.md, which was specified in an earlier plan, never written, and whose draft text still says "Gate 1b fails" — stale, Gate 1b passes at 0.928.

--- done, kept for the record ---
~~Confirm the Stage 2 design with the user (§4.6)~~ done — confirmed, built, run.
~~Record D-24 (Danenberg as arrival test) in the plan file~~ done.
~~Write pipeline2/nn/tokens.py and pipeline2/s2_tokens.py, then run Gate 2.~~ done — GATE 2 FAIL, see §4.6 and D-34.
