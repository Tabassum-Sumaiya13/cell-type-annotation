7. Known Gaps in the Stage 1b Build — open, not fixed
Recorded so the gate result is not read as a claim that every part of M1–M4 is doing work. All three were admitted to the user and are written into the plan file.

M1's co-expression and log-prevalence are computed, stored, and never used.
The co-expression matrix is written into work/s1b_signatures.npz and reaches containment as CX, but its only consumer is an optional coex_gate argument that nothing calls. Log prevalence is not in the distance at all.
The plan's M1 claims both are part of the signature, and its Cap-4 answer credits co-expression with rescuing thin marker overlap. Neither claim is currently supported by a measurement.
It is untested rather than disproven — no ablation was run. Cheap to settle: the ablation is one extra column. On this roster it should not matter much, because check 3 measured 100% of label pairs directly comparable, so the thin-overlap case co-expression exists for never actually fires. That changes when a marker-poorer cohort is added.

The nesting layer missed every case declared for it.
All three of nest_tcell, nest_treg, nest_tumour_ki67 failed to come out as nesting (two found no edge, one merged instead). They are required=0 so they do not block, and check 5 still reports 22 edges — but the edges it finds are not the ones predicted.
The nesting layer is the weakest part of the stage and must not be relied on by Stage 6's descendant-tolerant loss until re-tested.
Check 7's DAG assert (0 cycles, 0 transitivity violations) is correspondingly weak evidence: the graph is sparse enough that the M2b machinery had nothing to repair.

Stage 1b clustered all SIX cohorts together, so the frozen holdout helped shape the label space.
work/label_map.csv contains 9 ferguson labels, and ferguson's marker signatures were in the distance matrix that produced the 25 clusters. This is NOT a training leak on the classifier - ferguson cells have never entered any loss - but the label space Stage 7 will be scored in was not built blind to ferguson. A reviewer finds this in five minutes.
It is also not obviously avoidable: to score a new cohort at all you must map its labels into the cluster space, and doing that from its own marker signatures IS the deployment procedure this project claims. What is wrong is the ORDER - the clusters and ferguson's assignment were computed jointly, rather than freezing the clusters on 5 cohorts and then assigning ferguson into them.
SETTLED 2026-08-11 - the control was built (pipeline2/s1b_control.py) and RUN, and it FAILS: cell-weighted ARI 0.5447 against a declared 0.90. But the failure is granularity, not structure - at a matched cut the ARI is 0.9828 and the signatures are provably untouched. See D-46, H10 and reports/s1b_control_ferguson.md. Original plan below.
WAS: re-run the clustering on the 5 training cohorts, compare it to the current 25-cluster set restricted to those cohorts, and report the agreement. Small disagreement means ferguson barely moved the label space and the control retires the objection. Large disagreement is itself the finding. See H10.

--evidence-sweep and --graph-check do nothing.
Both are parsed into only and then ignored; every invocation produces the full report. The module docstring and the plan's verification command list both claim they subset it. Either wire them up or drop them.

8. Open Questions
8.1 High Priority (blocking research)
#	Question	Why blocking
H1	CLOSED 2026-08-11 — the [ABSENT] token STAYS, and the argument against it was wrong. Arm B (all 99 slots, learned [ABSENT]) beats Arm A (measured markers only) cross-cohort by 0.1963 vs 0.1670 on the 9 core markers, and Arm A goes NEGATIVE on the held-out-Sorin fold (−0.1033 vs +0.0388) — Sorin presents 12–17 tokens against the 39–57 the model trained on, which is exactly what an unseen panel looks like. Absent slots hold the token-set size at 99 for every cohort so the encoder never meets a set unlike anything in training. The cohort probe reached 1.000 for BOTH arms, so the fingerprint concern does not separate them and Stage 3's adversary inherits the same problem either way. Decided on the cross-cohort column, not the within-cohort one (D-30).	Was: determines Stage 2's architecture and how hard Stage 3 has to work.
H2	Does the nesting layer actually work? All three declared cases failed. ❌ ANSWERED 2026-08-14 BY STAGE 12 AND THE ANSWER IS NO, CONFIRMED FROM OUTSIDE (D-59). Gate 12 check 4b took all 493 label pairs the Cell Ontology relates as ancestor-descendant and asked whether Stage 1b represents them as EITHER a nesting edge OR a same-cluster merge. It represents 0.282 of them, against a bar of 0.60 declared before the run (0.377 on reliable clusters only). This is the first evidence of any kind about the layer and it is external, so the internal 3-case failure was not bad luck in the choice of cases.	Stage 6's descendant-tolerant loss depends on it. Cannot be built on an untrusted graph. ⚠️ NOW MEASURED, NOT SUSPECTED: any sentence in the write-up that claims a working parent-child structure must be removed, and Stage 6's descendant-tolerant loss must be described as operating on a graph that fails its external test.
H3	CLOSED 2026-08-11 — YES. Full panel 0.1963 · core-9 control 0.1707 · Gate 1 V3 0.169, all LOCO on the same 9 core markers. Both conditions hold: it clears 0.169, and it beats the core-9 control, which is the row that actually answers the question because the control is the SAME set transformer restricted to 9 markers (D-27) — so the only thing that changes is panel width, not architecture. Reported in two columns (D-33): as-scored 0.1963 and trained-only 0.2322, both passing, with the conservative one holding the verdict.	Was: if not, Stage 2's complexity is not earned and the whole masked-token design is in question.
H5	CLOSED 2026-08-11 — IT WAS A FAIR FIGHT. Matching the slide head to the probe's capacity made hiding WORSE: fresh bits at λ=0.3 rose to 2.92 (from 2.65 with the linear head) while the co-trained head fell to −4.53 (from −2.02). A stronger critic bought the encoder a better hiding place, not removal. Gate 3's negative result is not an artefact of a weak discriminator. See D-43. Original question below.	WAS: Was the adversary beaten, or was it never given a fair fight? `Heads.slide` is a single nn.Linear(128, n_slide); `SlideProbe` is a 2-hidden-layer 256-wide MLP. The encoder only had to make slide identity non-linearly separable. UNTESTED: rerun λ ∈ {0, 0.03, 0.1} with the slide head matched to the probe's architecture. ~25 min GPU.	Decides whether Stage 3's negative result is publishable. As it stands a reviewer closes it in one sentence. Either the adversary now removes information and F1 still does not improve — a clean finding — or F1 improves and there is real performance being left behind. See D-37.
H6	CLOSED 2026-08-11 — NO, AND THE HYPOTHESIS WAS WRONG. The nested slide-within-patient domain is worse than λ=0 at every lambda (−0.0183, −0.0306, −0.0857). The Sorin gain that motivated D-38 did not replicate: +0.0752 in Gate 3 became +0.0070 under nesting, while persisting at +0.0743 in the arm that does NOT nest. The n=5, p≈0.07 correlation did not survive its own test. Note also that arm B removed the MOST slide information proportionally (26.4% retained vs 38.0% at λ=0) and scored the WORST F1 — removing batch signal more successfully made transfer worse. See D-43. Original question below.	WAS: Is the adversary aimed at the wrong variable? In 4 of 5 folds most of its classes are individual patients (Keren 1.00 slides/patient, Sorin 1.13), so most of the adversarial loss asks the encoder to erase tumour biology. UNTESTED: make the domain SLIDE NESTED WITHIN PATIENT, so the discriminator only distinguishes slides sharing a patient — technical by construction. patient_id is already in the wide tables.	The strongest signal in the sweep points here: the one fold whose adversary is genuinely about batch gains +0.0752 while the patient-dominated folds gain 0.00–0.02, correlation −0.852. But n=5 and p≈0.07, so it is a hypothesis, not a result. See D-38.
H7	CLOSED 2026-08-11 — CONFIRMED exactly as this entry asked. The check it named ("re-score the saved checkpoints, no retraining") is now built as pipeline2/s8_perclass.py and run: learnable-only CRC 0.4640 · UPMC 0.4233 · Keren 0.4508 · Phillips 0.5219 · Sorin 0.4432, fold mean 0.4606 — CRC ranks 2nd of 5, not last. Gate 6 LOCO 0.3901 reported becomes 0.4606 learnable-only. The hypothesis below was written BEFORE the confirming measurement, which is what makes the confirmation worth anything. See D-53 (NOT D-48 — that reference in the text below was written before the D-numbers were assigned and is stale), reports/s8_perclass.md.
ORIGINAL HYPOTHESIS, kept verbatim: LIKELY EXPLAINED 2026-08-11, NOT YET CONFIRMED — it is mostly the METRIC, not CRC. `s3.macro_f1` averages over every cluster present in the held-out cohort, and 8 of the 25 clusters are cohort-exclusive, so under LOCO they are classes with ZERO training examples that score ~0 and are still averaged in. CRC has 5 such classes out of 14 scored, a hard ceiling of 0.643 before the model does anything; Sorin has none, ceiling 1.000. Dividing each fold by its own ceiling gives CRC 0.464 · UPMC 0.423 · Keren 0.451 · Phillips 0.522 · Sorin 0.443 — the folds are level and CRC is mid-pack, not the outlier. The generalisation gap noted below is partly the same artefact: the val number is scored on the TRAINING cohorts' class set, which contains no unseen classes at all, so val and test are not scoring the same thing. CONFIRM by re-scoring the saved checkpoints (~2 h, no retraining) before closing. See D-48. [STALE POINTER — the confirming decision is D-53.] Original question below.	WAS: the biggest single gap in the fold table, and NOT a batch problem, so no amount of Stage 3 work touches it. WAS: CRC is the WORST fold (0.2770) despite being the largest cohort with the second-richest panel (56 markers), still worst after Gate 6 at 0.2983 with the prototype head gaining only +0.0125 there, and with the largest generalisation gap on the roster — val 0.7866 against test 0.2983, +0.4883. Untested candidates were class imbalance (1,253:1 within CRC), label granularity, or its cluster mapping.
H8	NEW 2026-08-11 — The λ arms are not equally trained. Epochs before early stopping: λ=0 → 20,30,28,20,15; λ=0.3 → 7,7,8,7,8. With RAMP_EPOCHS=5, the λ=0.3 arm barely reaches full strength before patience-4 stops it.	Procedurally fair (same rule for every arm) but it means "λ=0 beats λ=0.3" is partly "23 epochs beats 7". Any rerun of the strong-λ arms should raise patience or lengthen the ramp before drawing a conclusion about them.
H4	NEW 2026-08-11 — Keren's held-out slides collapse the R² denominator for several markers, and Gate 2 check 1 fails on one of them (MKI67, −0.0117). Is this an unlucky 8-slide draw or a property of Keren? Untested: it would need the same run at a different slide-split seed.	Gate 6's masked-marker auxiliary loss inherits the same exclusion set and the same denominator problem, so this recurs. Also affects how much weight Keren's folds should carry in any cross-cohort mean.
H9	NEW 2026-08-11 — NOT ONE GATE IN THIS PROJECT HAS BEEN SCORED WITH A CONFIDENCE INTERVAL. Every verdict from Gate 1 onward is a mean over 5 folds, one seed, compared against a threshold with no paired test. Where the tests have now been run retrospectively they mostly do not survive: Gate 6 check 4 p = 0.460 with a CI four times the margin; Gate 6 check 3 (VICReg) p = 0.191; across Gate 3's whole lambda sweep the only significant contrast is lambda=0.3, which is WORSE. UNTESTED: re-run the shipped configuration at 5 seeds x 5 folds, about 3 h on a T4.	This is the single most damaging gap in the thesis. It does not change what ships - it changes which sentences can be written about what ships. Until it is fixed, every claim of the form 'X improves transfer' is unsupported, including Gate 6's own headline. See D-44.
H10	CLOSED 2026-08-11 by measurement - and the answer is BOTH WORSE AND NARROWER than expected. The control FAILS its declared thresholds (cell-weighted ARI 0.5447 against a 0.90 bar), so the 25-cluster space is NOT independent of the frozen holdout. But the mechanism is granularity, not structure: check 1 passes with the SIM/EV submatrices BIT-IDENTICAL, and at a matched cut the weighted ARI is 0.9828 with 18 of 22 control clusters nesting cleanly inside shipped ones. What ferguson changed is the CUT the guards allow - the usable window narrows from 0.650-0.850 to 0.750-0.800 - so the shipped partition is 25 clusters instead of 37. The shipped cut 0.800 is still INSIDE the 5-cohort window. Direction of the bias is known: coarser is easier, so it favours the result and must be stated beside Stage 7's number. See D-46, reports/s1b_control_ferguson.md	WAS: blocks Stage 7's headline being read as a clean zero-shot number. Still does, but the objection is now measured rather than open, which is what the control was for. It also threw off a separate defect that has nothing to do with ferguson - see D-47 and H11
H11	NEW 2026-08-11 — `choose_cut` picks the granularity by argmax of a stability curve whose whole range across the feasible window is 0.062, with the top two candidates 0.0035 apart. That is sampling a granularity, not selecting one. NOT REPAIRED - repairing it changes the shipped label space and every number since Gate 1b. See D-47.	Affects how the 25 clusters may be described in the write-up: as one defensible choice inside a feasible window, NOT as the granularity the data selected. Same failure family as H9 - a point estimate with no interval carrying a decision it cannot support.
H12	NEW 2026-08-11, from external review — THE LITERATURE HAS OVERTAKEN STAGES 2 AND 3, and no 2024-2026 competitor appears anywhere in files/. VirTues (Nature 2026) is marker tokens + masked-marker reconstruction + marker attention + protein-language-model marker identity, zero-shot across heterogeneous panels, at 5,100+ patients and 239 markers. DeepCell Types (2024) is a panel-agnostic channel transformer on 9.8M cells across 9 platforms, leave-one-DATASET-out accuracy 0.551 against MAPS 0.427. RIBCA (Cell Systems 2025) is panel-agnostic annotation with channel-masked marker imputation on 15M+ cells. CellHint (Cell 2023) is automatic ontology-free cell-type harmonisation, but on transcriptomes. What NONE of them do is derive the target label vocabulary from marker evidence — all four are handed it, and DeepCell Types uses an explicit human-in-the-loop name mapping, the artefact D-4 removed. See D-49.	Blocks the write-up, not the code. As framed today ("a panel-agnostic encoder for cross-cohort annotation") the thesis reads as a late reimplementation of published work. Reframed onto Stage 1b ("can the shared label vocabulary be DERIVED rather than curated") it has a contribution nobody has made in this modality. The related-work chapter is now required, and the 0.630 old-pipeline comparison must be dropped — it is a 3-class number against a 22-cluster one.
H13	PARTLY ACTIONED 2026-08-14 — both experiments are now BUILT, neither has a result yet. (a) is pipeline2/s10_external.py, MAPS reimplemented from its published Methods, 5 arms x 5 folds, running; its check 0 asserts the baseline sees byte-identical cells to the model and PASSES (D-57). (b) is pipeline2/s11_spacetest.py, built, needs a GPU (D-58) — and its build step already returned a result without training anything: on 3 cohorts NO cut satisfies Stage 1b's three hard guards, so granularity selection fails below 5 cohorts and only the matched-cut arm can answer the boundary question. ⚠️ NOTE THE THREE-DAY DELAY: this entry was written 2026-08-11 and Stages 8 and 9 were built before it. The idea was never missing; the sequencing was wrong. Original entry below.	WAS: NEW 2026-08-11, from external review — THERE IS NO EXTERNAL BASELINE, AND THE CENTRAL CLAIM HAS NEVER BEEN TESTED END TO END. Gate 3 check 1 compares against majority-class (0.0078) and random-uniform (0.0466); those are sanity checks a working model cannot fail, so 0.3901 is uninterpretable to a reader. Separately, the claim that learned label harmonisation replaces a hand-written one has only ever been measured as AGREEMENT between the two mappings (0.928), never as downstream performance. UNTESTED, both declared in D-50: (a) MAPS + gradient boosting on identical folds and the identical 25-cluster space; (b) the same model trained on the learned space vs on the hand mapping, LOCO over CRC/Keren/UPMC where both exist, paired and with an interval.	(b) is the single most important experiment left in the project — it is the thesis claim, tested as an outcome instead of as a clustering score. (a) is what makes every other number in the thesis readable. Neither needs new architecture.
H14	NEW 2026-08-14 — THE TRAINING ROSTER IS NOW DERIVED WRONGLY ON THIS MACHINE. `config.SPECS['Danenberg']` still carries `role='train'` from before D-24 repurposed it as the arrives-later cohort, and Stage 9 has now written it work/values/Danenberg_full.parquet. So `[c for c in s3.available() if SPECS[c]['role']=='train']` — the exact expression s3.main() and s6.main() use — returns SIX cohorts, not five. UNFIXED: needs `role` changed in config.py, which is an edit to a shipped file and was not made without approval. s10 and s11 pin the roster as a literal to work around it.	Any re-run of Gate 3 or Gate 6 on this machine would silently train on 6 cohorts and fail to reproduce the published numbers, with nothing in the log to signal it. It is the same failure family as D-39, where the vocabulary depended on which files were on disk rather than on the data.
H15	NEW 2026-08-14 — THE 25-CLUSTER LABEL SPACE HAS NO EXTERNAL VALIDATION OF ANY KIND. All eight Gate 1b checks are internal consistency (cohort guard, evidence coverage, LOCO split stability, coherence, DAG) or agreement with the project's OWN hand mapping (0.928). Not one compares the clusters to a published cell-type definition, an ontology, or another dataset's annotations. The gate passes and the gate tests nothing external. UNTESTED and cheap: `_validation/hand_mapping_reference.csv` ALREADY carries `cl_id` / `cl_name` — Cell Ontology terms for all 66 kept rows — and they have never been used as external evidence. HuBMAP's ASCT+B tables (Nature Cell Biology 2021; HRA v2.0, Nature Methods 2024; 1,195 cell types, 2,089 biomarkers, expert-curated) give a published consensus of cell type → biomarker that each cluster's top markers can be scored against. Cell Ontology's is_a hierarchy is the natural external test for the NESTING layer, which currently has none and failed all three of its declared cases. ✅ CLOSED 2026-08-14 BY STAGE 12 (D-59, reports/s12_ontology.md). The 25 clusters now have external validation: ARI 0.4400 [0.3662, 0.6624] against the exact Cell Ontology term, 0.5725 on reliable clusters only, 0.934 cell-weighted, over 101 of 106 labels and 33 CL terms — against the project's own hand mapping at 0.596 / 0.962 over 64 labels and 25 classes. Beats a 1000-shuffle null at p99.9 at all four of CL's OWN published levels. The nesting layer got its first external test and FAILED it (see H2). ⚠️ TWO THINGS THIS DOES NOT CLOSE: the CL term per label is still assigned by hand from each cohort's paper (only 0.228 resolved automatically), so the defensible sentence is "the derived clusters agree with a published ontology's STRUCTURE", never "the labels were mapped without human input"; and HuBMAP ASCT+B was unreachable, so CellMarker 2.0 stood in for the marker half.	Gate 1b is the stage the whole thesis is being reframed onto (D-49). A label space whose only validation is the author's own judgement cannot carry that weight, and this is the first thing an examiner will probe. No GPU, no training — table lookups against resources that already exist.
8.2 Medium Priority (important improvements)
#	Question
M6	Is the L1 agreement of 0.629 real or an artefact? Gate 1b scores agreement with the hand mapping at three levels: target (25 classes) 0.928, L2 (10) 0.855, L1 (3) 0.629. Agreement normally RISES as classes get coarser, so the fall is a red flag a reviewer will ask about. Most likely cause: `best_match_agreement` uses a one-to-one Hungarian assignment, and 25 clusters cannot be matched onto 3 classes without leaving 22 unmatched. Settle it by re-scoring L1 with a many-to-one mapping (each cluster to its majority L1 class). Two lines. If it jumps above 0.9 it was an artefact and should stop being reported as-is; if it does not, the clusters genuinely cut across immune/stromal/tumour and that is a problem for the central claim. ✅ SETTLED 2026-08-14 BY STAGE 12 CHECK 6 (D-59): IT WAS AN ARTEFACT. Many-to-one gives 0.9927 cell-weighted / 0.9219 per label against the reported 0.629. The same test at CL's own upper slim gives 0.9136 / 0.7100 over 17 classes. The clusters do NOT cut across immune/stromal/tumour. ⚠️ CONSEQUENCE FOR THE WRITE-UP: stop quoting 0.629 as a result. It is a Hungarian matching artefact and quoting it invites an objection that has already been answered.
M7	How much does the OPEN label space cost at inference? The classifier argmaxes over all 25 clusters, including clusters that cannot occur in the held-out cohort, so each impossible prediction costs precision on a class that is present. Oracle ablation: restrict the argmax to the clusters present in the held-out cohort and re-score. The gap is the cost of the open label space, and it sizes the value of Stage 7's abstain head before that head is built.
M8	Does training scale matter? Every reported number is on 40,000 cells per cohort — about 4% of the roster — while the published competitors train on 9.8M and 15M+ cells. Learning curve at 10k / 20k / 40k cells on one fold, ~20 min on a T4, before spending Kaggle hours on a full 4.9M run. If the curve is flat, scale is not the limit and the thesis can say so; if it is rising, the full run is worth the quota.
M1	Do co-expression and log-prevalence earn their place? One ablation column settles it (§7 gap 1). Should be run before a marker-poorer cohort is added, because that is when it starts to matter.
M2	Danenberg's three unresolved follow-ups — the duplicate HER2 triple policy, the CD31-vWF co-stain decision, and the bare-SMA → SMN1 alias risk (§3.3). All must be settled before the arrival test runs.
M3	What is the right tie_mass exclusion threshold for Stage 2? Declared at 0.5 with a sweep, but never measured.
M4	Will UPMC's assumed µm/px matter? Only Stage 4 (deferred) uses physical distance. Low risk while Stage 4 is off.
M5	Should _validation/ ever be deleted? Currently retained. The plan says delete after Gate 1b; the deviation is deliberate and documented.
8.3 Low Priority (future ideas)
#	Idea
L1	Report a Cell Ontology id per final cluster in the write-up only. Do not use CL anywhere in the pipeline, but reviewers compare across papers with those ids. Costs one lookup table at the end and keeps the pipeline pure.
L2	Wire up or delete --evidence-sweep / --graph-check (§7 gap 3).
L3	Revisit Stage 4 (spatial) after Stage 7 works.
L4	Revisit Stage 5 (global slide branch) as a final ablation only.
